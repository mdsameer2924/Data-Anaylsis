- ((69948ce0-7d64-40e4-bf0e-53a6782d8f58))
	- ```python
	  import numpy as np  #np is a numpy custom to give to use while accessing this module
	  oned_array=np.array([2,3,4]) ## numpy.array is an fuction 
	  ```
	- it's `numpy.array([])` is an function used to create an array in python