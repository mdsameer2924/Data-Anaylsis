- ```python
  months="janfebmaraprmayjunjulaugsepoctnovdec"  #12/4 = 5 
  num=int(input("Enter the number: "))
  
  #### print months name correcponing to their
  ##s=len(months)//num
  ##print(months[s::])
  
  x=(num-1)*3
  print(months[x:x+3])
        
  ```
- Question is take a user input of number from user of weeks days and enter number 1-7 and print days depend upon it & without conditional **statement** only using string slicing
  logseq.order-list-type:: number
- Adam number
  logseq.order-list-type:: number
	- logseq.order-list-type:: number
	  rev=num1[-1::-1]
	  ```python
	  num1=int(input("Enter the number "))    ## num -> square -> reverse -> -> square root -> reverse
	  
	  sq=num1**2
	  ## reverse 144 to 441
	  num1=str(num1)
	  # typecasete rev str to int
	  rev=int(rev)
	  num1=int(num1)
	  sqrt=sq**(0.5)
	  
	  
	  
	  print(num1)
	  print(sq)
	  print(sqrt)
	  print(type(rev))
	  
	  
	  ```
	-